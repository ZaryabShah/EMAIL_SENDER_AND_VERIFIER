# Standalone Email Pipeline Requirements
# Install these packages to run the standalone email verification and sending system

# DNS resolution for email verification
dnspython==2.7.0

# HTTP requests for proxy testing
requests==2.31.0

# Proxy support for SMTP connections (optional, for SOCKS proxies)
PySocks==1.7.1

# Email sending (built into Python, no additional packages needed)
# smtplib - built-in
# email - built-in
# ssl - built-in

# Core Python packages (usually built-in, but listed for completeness)
# re - built-in
# json - built-in  
# os - built-in
# random - built-in
# time - built-in
# typing - built-in
# datetime - built-in
# argparse - built-in
# sys - built-in
